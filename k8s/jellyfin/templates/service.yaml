apiVersion: v1
kind: Service
metadata:
  name: "{{ include "jellyfin.fullname" . }}"
spec:
  selector:
    {{- include "jellyfin.selectorLabels" . | nindent 6 }}
  ports:
  {{- range $key, $_ := .Values.ports }}
    - name: "{{ $key }}"
      targetPort: {{ .port }}
      port: {{ .port }}
  {{- end }}

