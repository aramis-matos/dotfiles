# https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ include "vpn.deployment" (dict "root" $ "name" "vpn") }}"
  namespace: "{{ .Release.Namespace }}"
spec:
  selector:
    matchLabels:
      {{- include "vpn.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "vpn.selectorLabels" . | nindent 8 }}
    spec:
      containers:
      - name: "{{ .Values.gluetun.containerName }}"
        image: "{{ .Values.gluetun.image.repository }}:{{ .Values.gluetun.image.tag }}"
        env:
          {{- include "vpn.load.env" .Values.gluetun.env | indent 10 }}
          - name: WIREGUARD_PRIVATE_KEY
            valueFrom:
              secretKeyRef:
                name: "{{ include "vpn.secret" (dict "root" $ "name" .Values.gluetun.secrets.name) }}"
                key: "{{ .Values.gluetun.secrets.privateKey.name }}"
          - name: WIREGUARD_ADDRESSES
            valueFrom:
              secretKeyRef:
                name: "{{ include "vpn.secret" (dict "root" $ "name" .Values.gluetun.secrets.name) }}"
                key: "{{ .Values.gluetun.secrets.addresses.name }}"
        ports:
          {{- include "vpn.load.ports" .Values.gluetun.ports | indent 10 }}
        volumeMounts:
          {{- include "vpn.load.volumeMounts" (dict "root" $ "name" "gluetun") | indent 10 }}
        securityContext:
          privileged: true
          capabilities:
            add:
              - NET_ADMIN
      - name: "{{ .Values.transmission.containerName }}"
        image: "{{ .Values.transmission.image.repository }}:{{ .Values.transmission.image.tag }}"
        env:
          {{- include "vpn.load.env" .Values.transmission.env | indent 10 }}
        ports:
          {{- include "vpn.load.ports" .Values.transmission.ports | indent 10 }}
        volumeMounts:
          {{- include "vpn.load.volumeMounts" (dict "root" $ "name" "transmission") | indent 10 }}
          - name: "{{ include "vpn.pvc" (dict "root" $ "name" .Values.transmission.configName) }}" 
            mountPath: /config
      - name: "{{ .Values.slskd.containerName }}"
        image: "{{ .Values.slskd.image.repository }}:{{ .Values.slskd.image.tag }}"
        env:
          {{- include "vpn.load.env" .Values.slskd.env | indent 10 }}
        ports:
          {{- include "vpn.load.ports" .Values.slskd.ports | indent 10 }}
        volumeMounts:
          {{- include "vpn.load.volumeMounts" (dict "root" $ "name" "slskd") | indent 10 }}
          - name: "{{ include "vpn.secret" (dict "root" $ "name" .Values.slskd.config.name ) }}"
            mountPath: "/app/slskd.yml"
            subPath: "{{ .Values.slskd.config.key }}"
      volumes:
      {{- range $key, $_ := .Values.volumes }}
        - name: "{{ $key }}"
          {{- .source | toYaml | nindent 10 }}
      {{- end }}
        - name: "{{ include "vpn.pvc" (dict "root" $ "name" .Values.transmission.configName) }}"
          persistentVolumeClaim:
            claimName: "{{ include "vpn.pvc" (dict "root" $ "name" .Values.transmission.configName) }}"
        - name: "{{ include "vpn.secret" (dict "root" $ "name" .Values.slskd.config.name) }}"
          secret:
            secretName: "{{ include "vpn.secret" (dict "root" $ "name" .Values.slskd.config.name) }}"
      restartPolicy: Always
---

